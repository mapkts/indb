[package]
name = "indb"
version = "0.1.0"
edition = "2018"

[dependencies]
tokio = { version = "1", features = ["full"] }
bytes = "1"

[dev-dependencies]
tokio = { version = "1", features = ["test-util"] }
